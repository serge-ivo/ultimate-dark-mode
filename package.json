{
  "name": "ultimate-dark-mode",
  "version": "0.1.0",
  "private": true,
  "description": "Dark mode for every website. AI-built, open source.",
  "type": "module",
  "scripts": {
    "test": "vitest run && node scripts/validate-overrides.js",
    "validate": "node scripts/validate-overrides.js",
    "test:watch": "vitest",
    "test:extension": "node scripts/test-extension.js",
    "test:headed": "node scripts/test-extension.js --headed",
    "test:keep": "node scripts/test-extension.js --keep"
  },
  "devDependencies": {
    "puppeteer": "^24.39.1",
    "vitest": "^3.1.0"
  }
}
